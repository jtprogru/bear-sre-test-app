# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Coverage output
*.out

# Build artifacts
dist/
bin/

# Local environment — SRV_ADDR и прочее. В репозиторий не попадает,
# образец лежит в .env.example.
.env
.env.*
!.env.example

# Локальный конфиг. Образец — config.example.yaml.
config.yaml

# Данные для /secret, генерируются `task testdata`
testdata/

# Editors / tooling
.idea/
.vscode/
.serena/
.DS_Store
