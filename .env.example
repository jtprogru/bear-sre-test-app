# Скопируйте в .env. Переменные перекрывают значения из config.yaml.
#
# Legacy-переменная: только порт.
SRV_ADDR=8080

# Канонический вид — префикс TESTAPP_ плюс путь ключа через подчёркивание.
# TESTAPP_PROD_PORT=8080
# TESTAPP_UPSTREAM_URL=https://example.com/
